export interface LicenseModel {
    id: number;
    start: string;
    end: string;
    active: number;
}

export interface LicenseStoreModel {
    companyID: number;
    startDate: string;
    endDate: string;
}
